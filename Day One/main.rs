fn main() {
    // I learned that rustc builds it and then we have to run the executable manually
    let x = 5;
    println!("the value of x is {}", x);

    let x = 6;
    println!("the value of x is {}", x);
}
